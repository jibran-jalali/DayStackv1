import { PomodoroWindowShell } from "@/components/app/pomodoro-window-shell";

export const metadata = {
  title: "Pomodoro Timer",
};

export default function PomodoroWindowPage() {
  return <PomodoroWindowShell />;
}
